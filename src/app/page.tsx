'use client';
import { Button } from "@diguinho_lns/test-ui";
import Image from "next/image";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      <Button
        text="primary"
        onClick={() => {}}
      />
    </main>
  );
}
